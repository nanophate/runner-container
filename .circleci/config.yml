version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: nanophate/vm-gcp-1
    steps:
      - run: echo "Hi I'm on Runners!"
      - run: sleep 500
